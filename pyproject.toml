[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xliff-nllb-translator"
version = "0.1.0"
dependencies = []

[project.scripts]
xliff-translator = "xliff_translator.cli:main"

[tool.setuptools.packages.find]
include = ["xliff_translator*"]
